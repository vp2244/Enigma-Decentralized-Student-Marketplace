\textbf{Owner: Member~4.} The \texttt{Reputation} contract maintains an on-chain rating ledger decoupled from escrow. \texttt{rateUser(listingId, rating)} lets the verified buyer of a completed (\emph{Sold}) listing assign a 1--5 score exactly once; an \texttt{AlreadyRated} guard blocks replays and out-of-range scores revert. \texttt{getAverageRating(user)} returns the running total and count for gas-efficient off-chain averaging. \texttt{Reputation} reads \texttt{Marketplace.getListing} to authenticate the rater, keeping the escrow and reputation slices cleanly separated. \emph{[Member~4: refine with events and the star-rating UI.]}
