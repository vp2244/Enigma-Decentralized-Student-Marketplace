\textbf{Owner: Member~2.} The \texttt{Marketplace} contract records item listings. \texttt{createListing} stores a \texttt{Listing} (seller, ENGC price, item metadata, status); \texttt{getListing(id)} and \texttt{totalListings()} expose listings to the UI. Input validation rejects empty metadata and non-positive prices, and each listing is assigned a monotonically increasing identifier. \emph{[Member~2: refine with the exact struct fields, events, and validation rules of the implemented contract.]}
