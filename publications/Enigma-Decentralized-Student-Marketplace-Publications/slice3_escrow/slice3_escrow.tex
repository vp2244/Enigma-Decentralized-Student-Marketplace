\textbf{Owner: Member~3.} Escrow custodies ENGC between purchase and delivery. \texttt{purchaseItem(id)} pulls the listing price via \texttt{transferFrom} into the contract --- the buyer must \texttt{approve} first --- and moves the listing to \emph{Pending}; \texttt{confirmDelivery} releases the held funds to the seller and marks the listing \emph{Sold}; \texttt{cancelPurchase} refunds the buyer. All token movements use \texttt{SafeERC20} and follow the checks-effects-interactions order under \texttt{ReentrancyGuard}, so state is updated before external calls. \emph{[Member~3: refine with the state machine, events, and dispute/timeout policy.]}
